/**
 * Bill extraction prompt — re-exports from the centralized prompt registry.
 * Keep this file so existing imports continue to resolve without changes.
 */
export { BILL_SYSTEM_PROMPT } from '../../prompts/extraction.prompts';
