/**
 * Excel extraction prompts — re-exports from the centralized prompt registry.
 * Keep this file so existing imports continue to resolve without changes.
 */
export { EXCEL_SYSTEM_PROMPT, EXCEL_HEADER_DETECTION_PROMPT } from '../../prompts/extraction.prompts';
