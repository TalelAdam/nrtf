# NRTF ml-pipeline — Python dependencies
# Tested with Python 3.11
#
# Install:  python -m venv .venv && .venv\Scripts\activate && pip install -r requirements.txt
# (on macOS/Linux: source .venv/bin/activate)

# --- Data wrangling ---
pandas>=2.2,<3.0
polars>=1.0,<2.0          # fast lazy/streaming engine
pyarrow>=16.0             # parquet read/write backend used by both pandas and polars
openpyxl>=3.1             # .xlsx read/write (pandas/polars use it under the hood)
xlsxwriter>=3.2           # nicer per-sensor Excel exports (formatting)
python-dateutil>=2.9
unidecode>=1.3            # strip accents from French sensor names for safe filenames

# --- Visualisation (EDA) ---
matplotlib>=3.9
seaborn>=0.13
plotly>=5.22              # for the sankey + interactive html reports

# --- Modelling ---
scikit-learn>=1.5         # IsolationForest, baselines, train/test split
lightgbm>=4.5             # forecaster of choice
statsmodels>=0.14         # STL decomposition, simple ARIMA baseline
holidays>=0.55            # Tunisian holiday flags as a calendar feature

# --- Serving ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8

# --- Experiment tracking / MLOps ---
mlflow>=2.16

# --- Dev / quality ---
ruff>=0.6
pytest>=8.3
