import { DocumentPipeline } from "@/components/documents/document-pipeline";

export const metadata = {
  title: "Document Pipeline · ReTeqFusion",
  description: "Heterogeneous document extraction and submission",
};

export default function DocumentsPage() {
  return <DocumentPipeline />;
}
