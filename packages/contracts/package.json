{
  "name": "@nrtf/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Solidity smart contracts (Hardhat) — only used if the team picks the fintech track (SolREC-Tn / SoukWatt / PaySun)",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:amoy": "hardhat run scripts/deploy.ts --network amoy"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "@types/node": "^22.7.4",
    "hardhat": "^2.22.13",
    "typescript": "^5.5.4",
    "viem": "^2.21.16"
  }
}
